sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t jmcausing/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true jmcausing/docker-react npm run test -- --coverage
